rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالقراءة والكتابة لجميع المستندات مؤقتًا للتطوير
    // سيتم تأمين هذه القواعد لاحقًا
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
